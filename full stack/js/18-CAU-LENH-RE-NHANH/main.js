// câu lệnh rẽ nhánh
// so sánh câu sử dụng toán từ (===)
let nhapThu = Number(prompt("Mời nhập thứ: "))

switch (nhapThu) {
    case 2: // case trường hợp so sánh, nếu biến = 2 thì thực thi
        console.log('Hôm nay là thứ hai');
        break; // thoát thỏi box switch
}
switch (nhapThu) {
    case 3: // case trường hợp so sánh3, nếu biến = 2 thì thực thi
        console.log('Hôm nay là thứ ba');
        break; // thoát thỏi box switch
}
switch (nhapThu) {
    case 4: // case trường hợp so sánh, nếu biến = 2 thì thực thi
        console.log('Hôm nay là thứ 4');
        break; // thoát thỏi box switch
}
switch (nhapThu) {
    case 5: // case trường hợp so sánh, nếu biến = 2 thì thực thi
        console.log('Hôm nay là thứ 5');
        break; // thoát thỏi box switch
}